@extends("base")

@section('sidebar')
    @parent
    <h1>Book Issued</h1>
@endsection

@section('content')
    <div class="box box-primary">
        <div class="box-header">
            <h4>Edit Book Issued</h4>

            @if(!empty($errors->all()) or !empty(Session::get('error')))
                <div class="alert alert-danger" role="alert">
                    {{Session::get('error')}}
                    <ul>
                        @foreach($errors->all() as $error)
                            <li>{{ $error }}</li>
                        @endforeach
                    </ul>
                </div>
            @endif

            @if(!empty(Session::get('success')))
                <div class="alert alert-success" role="alter">
                    {{Session::get('success')}}
                </div>
            @endif

        </div>
        <div class="box-body">

            {!! Form::open(array('url'=>'book_issued/' . $data->bi_id,'method' => 'PUT', 'class' => 'form-horizontal', 'files'=>true))!!}
            <div class="form-group">
                <label class="col-xs-2 control-label">Book Title</label>
                <div class="col-xs-4">
                    {!! Form::select('book_id', $books, $data->book_id, array('class' => 'form-control input-sm js-select2')) !!}
                </div>
                <label class="col-xs-2 control-label">Employee</label>
                <div class="col-xs-4">
                    {!! Form::select('emp_id', $employees, $data->emp_id, array('class' => 'form-control input-sm js-select2')) !!}
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-2 control-label">Issue Date</label>
                <div class="col-xs-4">
                    {!! Form::text('date_issue', ($data->date_issue) ? date('d-m-Y', strtotime($data->date_issue)) : null, array('class' => 'form-control input-sm dateMask', 'placeholder' => 'dd-mm-yyyy', 'data-inputmask' => "'alias': 'dd-mm-yyyy'")) !!}
                </div>
                <label class="col-xs-2 control-label">Return Date</label>
                <div class="col-xs-4">
                    {!! Form::text('date_return', ($data->date_return) ? date('d-m-Y', strtotime($data->date_return)) : null, array('class' => 'form-control input-sm dateMask', 'placeholder' => 'dd-mm-yyyy', 'data-inputmask' => "'alias': 'dd-mm-yyyy'")) !!}
                </div>
            </div>
            <div class="form-group">
                <label class="col-xs-2 control-label">E-Document</label>
                <div class="col-xs-4">
                    {!! Form::file('bi_edoc') !!}
                </div>
            </div>

            <button type="submit" class="btn btn-primary pull-right">Update</button>
            {!! Form::close()!!}
        </div>
    </div>

@stop