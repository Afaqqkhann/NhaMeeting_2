@extends("base")

@section('sidebar')
@parent
    <h1>Books</h1>
@endsection

@section('content')

    @if(!empty(Session::get('success')))
        <div class="alert alert-success" role="alter">
            {{Session::get('success')}}
        </div>
    @endif

<div class="box  box-primary">
    <div class="box-header">
        <h3 class="box-title">Books</h3>
        <a href="{{ URL::to('books/create') }}" class="btn btn-primary btn-sm btn-success pull-right">Add Book</a>
    </div><!-- /.box-header -->
    <div class="box-body">
          <table class="table table-bordered table-striped theDataTable">
            <thead  class="bg-gray">
              <tr>
                <th>#</th>
                <th>Book Title</th>
                <th>Author 1</th>
                <th>Author 2</th>
                <th>Receive Dated</th>
                <th>Publish Date</th>
                <th>Bill No</th>
                <th>Purchase Date</th>
                <th>Cost</th>
                <th>Source Name</th>
                <th>Place Name</th>
                <th>Publisher Name</th>
                <th>Subcategory</th>
                <th>Document</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              @foreach($data as $key => $row)
              <tr>
                <td>{{ $key + 1 }}</td>
                <td>{{ $row->book_title }}</td>
                <td>{{ $row->auther_1 }}</td>
                <td>{{ $row->auther_2 }}</td>
                <td>{{ ($row->rec_date) ? date('d-M-Y', strtotime($row->rec_date)) : null }}</td>
                <td>{{ ($row->publish_date) ? date('d-M-Y', strtotime($row->publish_date)) : null }}</td>
                <td>{{ $row->bill_no}}</td>
                <td>{{ ($row->purchase_date) ? date('d-M-Y', strtotime($row->purchase_date)) : null }}</td>
                <td>{{ number_format($row->cost)}}</td>
                <td>{{ $row->source_name}}</td>
                <td>{{ $row->place_name}}</td>
                <td>{{ $row->publisher_name}}</td>
                <td>{{ $row->subcategory->sc_title}}</td>
                <td>{{ $row->edoc }}</td>
                <td>
                    <a href="{{ URL::to('books') . '/' . $row->book_id }}" class="btn btn-xs pull-left">
                        <i class="fa fa-eye "></i>
                    </a>
                    <a href="{{ URL::to('books') . '/' . $row->book_id . '/edit' }}" class="btn btn-xs pull-left">
                        <i class="fa fa-pencil "></i>
                    </a>
                    {!! Form::open(['url'=>'/books/'.$row->book_id, 'class'=>'pull-left']) !!}
                    {!! Form::hidden('_method', 'DELETE') !!}
                    <button class="btn btn-xs">
                        <i class="fa fa-times text-danger"></i>
                    </button>
                    {!! Form::close() !!}
                </td>
              </tr>
              @endforeach
            </tbody>
           </table>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop