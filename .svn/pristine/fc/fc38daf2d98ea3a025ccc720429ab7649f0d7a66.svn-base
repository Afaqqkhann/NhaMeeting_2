@extends("base")

@section('sidebar')
@parent
    <h1>Book Issued</h1>
@endsection

@section('content')

<div class="box  box-primary">
    <div class="box-body">
          <table class="table table-bordered table-striped dtable dataTables">
            <tr>
                <th>Book Title</th><td>{{ $data->book->book_title }}</td>
            </tr>
            <tr>
                <th>Employee</th><td>{{ $data->employee->emp_name }}</td>
            </tr>
            <tr>
                <th>Issued Date</th><td>{{ ($data->date_issue) ? date('d-M-Y', strtotime($data->date_issue)) : null }}</td>
            </tr>
            <tr>
                <th>Return Date</th><td>{{ ($data->date_return) ? date('d-M-Y', strtotime($data->date_return)) : null }}</td>
            </tr>

            <tr>
                <th>E-Document</th>
                @if($data->bi_edoc)
                <td>
                    <a href="{{url('storage/book_issued/'.$data->bi_edoc)}}" title="Document"><i class="fa fa-file"></i></a>
                </td>
                @endif
            </tr>

          </table>

          <a href="{{ URL::to('book_issued') }}" class="btn btn-primary">Back</a>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop