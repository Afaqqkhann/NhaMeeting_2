@extends("base")

@section('sidebar')
    @parent
    <h1>Edit Organization</h1>
@endsection

@section('content')
    <div class="box box-primary">
        <div class="box-header">
            <h4>Edit Organization</h4>

            @if(!empty($errors->all()) or !empty(Session::get('error')))
                <div class="alert alert-danger" role="alert">
                    {{Session::get('error')}}
                    <ul>
                        @foreach($errors->all() as $error)
                            <li>{{ $error }}</li>
                        @endforeach
                    </ul>
                </div>
            @endif

            @if(!empty(Session::get('success')))
                <div class="alert alert-success" role="alter">
                    {{Session::get('success')}}
                </div>
            @endif

        </div>
        <div class="box-body">

            {!! Form::open(array('url'=>'organization/' . $data->org_id,'method' => 'PUT', 'class' => 'form-horizontal', 'files'=>true))!!}
            <div class="form-group">
                <label class="col-xs-2 control-label">Organization Name</label>
                <div class="col-xs-10">
                    {!! Form::text('org_name', $data->org_name, array('class' => 'form-control input-sm')) !!}
                </div>
            </div>

            <button type="submit" class="btn btn-primary pull-right">Update</button>
            {!! Form::close()!!}
        </div>
    </div>

@stop