@extends("base")

@section('sidebar')
@parent
    <h1>Book Issued</h1>
@endsection

@section('content')

    @if(!empty(Session::get('success')))
        <div class="alert alert-success" role="alter">
            {{Session::get('success')}}
        </div>
    @endif

<div class="box  box-primary">
    <div class="box-header">
        <h3 class="box-title">Book Issued</h3>
        <a href="{{ URL::to('book_issued/create') }}" class="btn btn-primary btn-sm btn-success pull-right">Add Book Issued</a>
    </div><!-- /.box-header -->
    <div class="box-body">
          <table class="table table-bordered table-striped theDataTable">
            <thead  class="bg-gray">
              <tr>
                <th>#</th>
                <th>Book Title</th>
                <th>Employee</th>
                <th>Issued Date</th>
                <th>Return Date</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              @foreach($data as $key => $row)
              <tr>
                <td>{{ $key + 1 }}</td>
                <td>{{ $row->book->book_title }}</td>
                <td>{{ $row->employee->emp_name }}</td>
                <td>{{ ($row->date_issue) ? date('d-M-Y', strtotime($row->date_issue)) : null }}</td>
                <td>{{ ($row->date_return) ? date('d-M-Y', strtotime($row->date_return)) : null }}</td>
                <td>
                    <a href="{{ URL::to('book_issued') . '/' . $row->bi_id }}" class="btn btn-xs pull-left">
                        <i class="fa fa-eye "></i>
                    </a>
                    <a href="{{ URL::to('book_issued') . '/' . $row->bi_id . '/edit' }}" class="btn btn-xs pull-left">
                        <i class="fa fa-pencil "></i>
                    </a>
                    {!! Form::open(['url'=>'/book_issued/'.$row->bi_id, 'class'=>'pull-left']) !!}
                    {!! Form::hidden('_method', 'DELETE') !!}
                    <button class="btn btn-xs">
                        <i class="fa fa-times text-danger"></i>
                    </button>
                    {!! Form::close() !!}
                </td>
              </tr>
              @endforeach
            </tbody>
           </table>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop