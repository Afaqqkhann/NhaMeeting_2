@extends('base')

@section('content')
		
		@if($errors->first())
			<div class="alert alert-danger">
				{{ $errors->first() }}
			</div>
		@endif
		
		<div class="box box-primary">
              <div class="box-header">
                 <h4>Edit User</h4>
              </div>
              <div class="box-body">
               {!! Form::open(array('url' => 'user/' . $user->id, 'method' => 'PUT', 'class' => 'form-horizontal')) !!}
                  <div class="form-group">
                      <label class="col-sm-2 control-label">Name</label>
                      <div class="col-sm-4">
                          {!! Form::text('name', $user->name, array('class' => 'form-control input-sm', 'readonly' => 'readonly')) !!}
                      </div>
                  </div>
				  
                 <div class="form-group">
                     <label class="col-sm-2 control-label">Role</label>
                     <div class="col-sm-4">
                        {!! Form::select('role_id', $roles, $user->roles->first()->id, array('class' => 'form-control input-sm')) !!}
                     </div>
                 </div>

                 <button type="submit" class="btn btn-primary pull-right">Save</button>
               {!! Form::close()!!}
              </div>
          </div>
		  
@stop