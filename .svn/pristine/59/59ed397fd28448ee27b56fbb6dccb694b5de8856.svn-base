@extends("base")

@section('sidebar')
@parent
    <h1>Trainings HRD</h1>
@endsection

@section('content')

<div class="box  box-primary">
    <div class="box-body">
          <table class="table table-bordered table-striped dtable dataTables">
            <tr>
                <th>Course Title</th><td>{{ $data->course_title }}</td>
            </tr>
           {{-- <tr>
                <th>Duration (Days)</th><td>{{ $data->duration_days }}</td>
            </tr>--}}
            <tr>
                <th>Start Date</th><td>{{ ($data->start_dated) ? date('d-M-Y', strtotime($data->start_dated)) : null }}</td>
            </tr>
            <tr>
                <th>Apply Date</th><td>{{ ($data->apply_dated) ? date('d-M-Y', strtotime($data->apply_dated)) : null }}</td>
            </tr>
            <tr>
                <th>End Date</th><td>{{ ($data->end_dated) ? date('d-M-Y', strtotime($data->end_dated)) : null }}</td>
            </tr>
            <tr>
                <th>Training LOC</th><td>{{ $data->training_loc }}</td>
            </tr>
            <tr>
                <th>E-Document</th>
                <td>
                    <a href="{{url('storage/trainings_hrd/'.$data->edoc)}}" title="Document"><i class="fa fa-file"></i></a>
                </td>
            </tr>
            <tr>
                <th>Comments</th><td>{{ $data->comments }}</td>
            </tr>
          </table>

          <a href="{{ URL::to('package_ipc') }}" class="btn btn-primary">Back</a>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop