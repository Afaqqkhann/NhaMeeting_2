@extends("base")

@section('sidebar')
@parent
    <h1>Subcategories</h1>
@endsection

@section('content')

    @if(!empty(Session::get('success')))
        <div class="alert alert-success" role="alter">
            {{Session::get('success')}}
        </div>
    @endif

<div class="box  box-primary">
    <div class="box-header">
        <h3 class="box-title">Subcategories</h3>
        <a href="{{ URL::to('subcategory/create') }}" class="btn btn-primary btn-sm btn-success pull-right">Add Subcategory</a>
    </div><!-- /.box-header -->
    <div class="box-body">
          <table class="table table-bordered table-striped theDataTable">
            <thead  class="bg-gray">
              <tr>
                <th>#</th>
                <th>Subcategory Title</th>
                <th>Category</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              @foreach($data as $key => $row)
              <tr>
                <td>{{ $key + 1 }}</td>
                <td>{{ $row->sc_title }}</td>
                <td>{{ $row->category->category_title }}</td>
                <td>
                    <a href="{{ URL::to('subcategory') . '/' . $row->sc_id }}" class="btn btn-xs pull-left">
                        <i class="fa fa-eye "></i>
                    </a>
                    <a href="{{ URL::to('subcategory') . '/' . $row->sc_id . '/edit' }}" class="btn btn-xs pull-left">
                        <i class="fa fa-pencil "></i>
                    </a>
                    {!! Form::open(['url'=>'/subcategory/'.$row->sc_id, 'class'=>'pull-left']) !!}
                    {!! Form::hidden('_method', 'DELETE') !!}
                    <button class="btn btn-xs">
                        <i class="fa fa-times text-danger"></i>
                    </button>
                    {!! Form::close() !!}
                </td>
              </tr>
              @endforeach
            </tbody>
           </table>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop