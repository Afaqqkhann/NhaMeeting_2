@extends("base")

@section('sidebar')
@parent
    <h1>Order</h1>
@endsection

@section('content')

<div class="box  box-primary">
    <div class="box-body">
          <table class="table table-bordered table-striped dtable dataTables">
            <tr>
                <th>Order No.</th><td>{{ $data->order_no or ''}}</td>
            </tr>
           {{-- <tr>
                <th>Duration (Days)</th><td>{{ $data->duration_days }}</td>
            </tr>--}}
            <tr>
                <th>Order Date</th><td>{{ ($data->order_date) ? date('d-M-Y', strtotime($data->order_date)) : null }}</td>
            </tr>
              <?php if($data->order_type == 1)
                  $order = 'Order';
              elseif($data->order_type == 2)
                  $order = 'Circular';
              elseif($data->order_type == 3)
                  $order = 'Letter';
              else if($data->order_type == 4)
                  $order = 'Noting'; ?>
            <tr>
                <th>Order Type</th><td>{{ $order or '' }}</td>
            </tr>
            <tr>
                <th>Subject</th><td>{{ $data->order_subject or '' }}</td>
            </tr>
              <tr>

                  <th>Section</th><td>{{ $section->section_name or ''}}</td>
              </tr>
              <tr>
                  <th>Approved Authority</th><td>{{ $app_auth->aa_name or '' }}</td>
              </tr>
            <tr>
                <th>E-Document</th>
                <td>
                    @if($data->e_doc)
                        <a target="_blank" href="{{ URL::to($data->e_doc)}}" class="btn btn-primary btn-sm btn-social" ><i class="fa fa-eye"></i> View</a>
                        @endif
                </td>
            </tr>
          </table>

          <a href="{{ URL::to('order') }}" class="btn btn-primary">Back</a>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop