@extends("base")

@section('sidebar')
@parent
    <h1>Trainings HRD</h1>
@endsection

@section('content')

    @if(!empty(Session::get('success')))
        <div class="alert alert-success" role="alter">
            {{Session::get('success')}}
        </div>
    @endif

<div class="box  box-primary">
    <div class="box-header">
        <h3 class="box-title">Trainings HRD</h3>
        <a href="{{ URL::to('trainings_hrd/create') }}" class="btn btn-primary btn-sm btn-success pull-right">Add Trainings HRD</a>
    </div><!-- /.box-header -->
    <div class="box-body">
          <table class="table table-bordered table-striped theDataTable">
            <thead  class="bg-gray">
              <tr>
                <th>#</th>
                <th>Course Title</th>
                <th>Duration Days</th>
                <th>Start Dated</th>
                <th>Apply Dated</th>
                <th>Training LOC</th>
                <th>Comments</th>
                <th>Document</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              @foreach($data as $key => $row)
              <tr>
                <td>{{ $key + 1 }}</td>
                <td>{{ $row->course_title }}</td>
                <td>{{ $row->duration_days }}</td>
                <td>{{ ($row->start_dated) ? date('d-M-Y', strtotime($row->start_dated)) : null }}</td>
                <td>{{ ($row->apply_dated) ? date('d-M-Y', strtotime($row->apply_dated)) : null }}</td>
                <td>{{ $row->training_loc}}</td>
                <td>{{ $row->comments}}</td>
                <td>{{ $row->edoc }}</td>
                <td>
                    <a href="{{ URL::to('trainings_hrd') . '/' . $row->th_id }}" class="btn btn-xs pull-left">
                        <i class="fa fa-eye "></i>
                    </a>
                    <a href="{{ URL::to('trainings_hrd') . '/' . $row->th_id . '/edit' }}" class="btn btn-xs pull-left">
                        <i class="fa fa-pencil "></i>
                    </a>
                    {!! Form::open(['url'=>'/trainings_hrd/'.$row->th_id, 'class'=>'pull-left']) !!}
                    {!! Form::hidden('_method', 'DELETE') !!}
                    <button class="btn btn-xs">
                        <i class="fa fa-times text-danger"></i>
                    </button>
                    {!! Form::close() !!}
                </td>
              </tr>
              @endforeach
            </tbody>
           </table>
    </div><!-- /.box-body -->
</div><!-- /.box -->

@stop